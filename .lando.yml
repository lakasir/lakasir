name: lakasir
recipe: laravel
services:
  appserver:
    xdebug: false
  database:
    portforward: 3305
config:
  via: nginx
  php: '8.1'
  webroot: public
  composer_version: '2.2.12'
  database: mysql

